name = "suno-api-workers"
main = "src/index.ts"
compatibility_date = "2025-03-05"
compatibility_flags = ["nodejs_compat_v2"]

[env.production]
vars = { ENVIRONMENT = "production" }

[browser]
binding = "MYBROWSER"

# Environment variables are loaded from .dev.vars file for security
# Do not commit secrets to wrangler.toml